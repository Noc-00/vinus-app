# Nombre del proyecto
spring.application.name=vinus-server

# Configuración de Neon (PostgreSQL)
spring.datasource.url=jdbc:postgresql://ep-misty-hall-am7i5tdj-pooler.c-5.us-east-1.aws.neon.tech/vinus_db?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_CU08Rjbvuctq
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Archivos y recursos
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

server.port=${PORT:10000}